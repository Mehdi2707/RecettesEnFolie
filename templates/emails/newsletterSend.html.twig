<div style="text-align: center;">
    {% set imagePath = absolute_url(asset('assets/img/core-img/logo.webp')) %}
    <img src="{{ imagePath }}" alt="Logo" width="305" style="display: block; margin: 0 auto;" />
</div>
<h1>{{ newsletter.name }}</h1>
<div>
    {{ newsletter.content|raw }}
</div>

<p>Pour vous désinscrire, cliquez sur ce lien <a href="{{ absolute_url(path('unsubscribe_newsletter', {id: user.id, token: user.validationToken})) }}">Me désinscrire</a></p>